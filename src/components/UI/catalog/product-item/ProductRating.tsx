import { IReview } from '@/types/review.interface'
import { FC } from 'react'

const ProductRating: FC<{reviews: IReview[]}> = ({reviews}) => {
  return <div>ProductRating</div>
}

export default ProductRating